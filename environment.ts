/**
 * Environment object to configure how the ui executes.
 */
export const environment = {
    /** If true, skips user input and uses AI behavior for the user's characters. */
    skipUserInput: false,
};
